what if i want to have more than one scraper (that is, one for each of the two websites)?

AI suggested BeautifulSoup. I chose scrapy, having in mind scalability and performance.
AI suggested I keep it simple, scraping a single site or even a single item. I put Scrapy to the test on 3 different websites.
AI suggested to put the docker-compose.yml inside .devcontainers. Followed the prompt, but pulled it to root when preparing Airflow
AI suggested hardcoded credentials. I corrected them, given the security risks.
AI suggested Airflow 2.7.1. I chose Airflow 3.1.5.
AI suggested wrong import paths. I had to check the official documentation to correct it (also corrected dags and logs path).
I decided to delete all my images and volumes and rebuild all containers from the bottom up to check if it still worked.